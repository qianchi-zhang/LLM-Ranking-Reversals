\documentclass[10pt,aspectratio=169]{beamer}
\usetheme{Madrid}
\setbeamertemplate{navigation symbols}{}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta}

\definecolor{Ink}{HTML}{17324D}
\definecolor{Accent}{HTML}{C8553D}
\definecolor{Soft}{HTML}{F5F1E8}
\definecolor{Deep}{HTML}{0F2233}
\definecolor{Muted}{HTML}{5C6B73}
\definecolor{Good}{HTML}{2A9D8F}

\setbeamercolor{title}{fg=white,bg=Ink}
\setbeamercolor{frametitle}{fg=white,bg=Ink}
\setbeamercolor{structure}{fg=Ink}
\setbeamercolor{block title}{fg=white,bg=Ink}
\setbeamercolor{block body}{fg=black,bg=Soft}
\setbeamercolor{itemize item}{fg=Accent}
\setbeamercolor{enumerate item}{fg=Accent}

\newcommand{\sectionframe}[2]{
  \begin{frame}[plain]
    \begin{tikzpicture}[remember picture,overlay]
      \fill[Ink] (current page.south west) rectangle (current page.north east);
    \end{tikzpicture}
    \vspace{1.7cm}
    {\color{white}\Huge \textbf{#1}}\\[0.45cm]
    {\color{white}\Large #2}
  \end{frame}
}

\newcommand{\insightbox}[1]{
  \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
  \centering \large \textbf{#1}
  \end{beamercolorbox}
}

\newcommand{\metriccard}[2]{
  \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.7em,wd=\linewidth]{block body}
    \centering
    {\color{Accent}\fontsize{20}{22}\selectfont \textbf{#1}}\\[0.3em]
    {\small #2}
  \end{beamercolorbox}
}

\title{Do LLM Rankings Change if You Ask Differently?}
\subtitle{Ranking Reversals under Non-semantic Prompt Perturbations and Subset Resampling}
\author{ST5230 Group 14\\ZHANG \and PENG \and JIANG \and MENG}
\date{}

\begin{document}

\begin{frame}[plain]
  \begin{tikzpicture}[remember picture,overlay]
    \fill[Ink] (current page.south west) rectangle (current page.north east);
  \end{tikzpicture}
  \vspace{1.1cm}
  {\color{white}\Huge \textbf{Do LLM Rankings Change}}\\[0.12cm]
  {\color{white}\Huge \textbf{if You Ask Differently?}}\\[0.55cm]
  {\color{white}\large Ranking reversals under non-semantic prompt perturbations}\\[0.1cm]
  {\color{white}\large and subset resampling}\\[1.2cm]
  {\color{white}\normalsize ST5230 Group 14}
\end{frame}

\begin{frame}{A Familiar Story, and the Question Behind It}
  \begin{columns}[T]
    \begin{column}{0.46\textwidth}
      \onslide<1->{
      \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.9em,wd=\linewidth]{block body}
        {\large \textbf{What a leaderboard suggests}}\\[0.5em]
        \begin{itemize}
          \item Model A: 89.2\%
          \item Model B: 87.3\%
          \item Therefore, A is better than B
        \end{itemize}
      \end{beamercolorbox}
      }
    \end{column}
    \begin{column}{0.5\textwidth}
      \onslide<2->{
      \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.9em,wd=\linewidth]{block body}
        {\large \textbf{What we ask instead}}\\[0.5em]
        \begin{itemize}
          \item What if we ask the same question differently?
          \item What if we evaluate on a slightly different subset?
          \item Does the rank order remain the same?
        \end{itemize}
      \end{beamercolorbox}
      }
    \end{column}
  \end{columns}

  \onslide<3->{
    \vspace{0.9em}
    \insightbox{Our target is not only score accuracy, but the reliability of the ranking itself.}
  }
\end{frame}

\begin{frame}{Motivation}
  \begin{itemize}[<+->]
    \item Static leaderboards are attractive because they compress a benchmark into a single ordering.
    \item But that ordering implicitly assumes \textbf{evaluation stability}: small, reasonable protocol changes should not alter the winner.
    \item In practice, benchmark outcomes depend on at least two nontrivial factors:
    \begin{itemize}
      \item prompt formulation
      \item evaluation subset composition
    \end{itemize}
    \item If these sources of variation are large relative to model gaps, rank claims may be overconfident.
  \end{itemize}

  \onslide<5->{
    \vspace{0.8em}
    \insightbox{We study when a leaderboard is robust, and when it is only an artifact of one evaluation choice.}
  }
\end{frame}

\begin{frame}{Positioning Relative to Prior Work}
  \begin{columns}[T]
    \begin{column}{0.31\textwidth}
      \onslide<1->{
      \metriccard{Benchmark papers}{MMLU, ARC, and HellaSwag establish the task settings we evaluate.}
      }
    \end{column}
    \begin{column}{0.31\textwidth}
      \onslide<2->{
      \metriccard{Prompt robustness}{Prior work shows that task-preserving prompt changes can affect LLM behavior.}
      }
    \end{column}
    \begin{column}{0.31\textwidth}
      \onslide<3->{
      \metriccard{Evaluation reliability}{Recent discussions emphasize that protocol matters, not just model identity.}
      }
    \end{column}
  \end{columns}

  \onslide<4->{
    \vspace{0.9em}
    \begin{block}{Our contribution}
      We focus on \textbf{ranking stability}: not just whether scores move, but whether the order between models reverses.
    \end{block}
  }
\end{frame}

\begin{frame}{Research Questions}
  \begin{enumerate}[<+->]
    \item Do non-semantic prompt perturbations change model performance?
    \item Does evaluation-subset variation change relative model rankings?
    \item Are those changes large enough to create \textbf{ranking reversals}?
  \end{enumerate}

  \onslide<4->{
    \vspace{0.9em}
    \begin{columns}[T]
      \begin{column}{0.48\textwidth}
        \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
          {\large \textbf{If the answer is no}}\\[0.4em]
          A static leaderboard is a reasonable summary.
        \end{beamercolorbox}
      \end{column}
      \begin{column}{0.48\textwidth}
        \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
          {\large \textbf{If the answer is yes}}\\[0.4em]
          Rank claims should be reported with uncertainty and robustness diagnostics.
        \end{beamercolorbox}
      \end{column}
    \end{columns}
  }
\end{frame}

\sectionframe{Method}{Treat ranking as a random outcome induced by evaluation choices}

\begin{frame}{Core Idea and Theoretical Framing}
  \begin{columns}[T]
    \begin{column}{0.56\textwidth}
      \begin{itemize}[<+->]
        \item Keep task semantics fixed.
        \item Perturb the prompt in a controlled, non-semantic way.
        \item Resample the evaluation subset via bootstrap.
        \item Recompute the ranking under each induced evaluation configuration.
      \end{itemize}

      \onslide<3->{
        \vspace{0.5em}
        \[
        r_{d,p,b}(m) = \text{rank of model } m
        \]
        \[
        \text{RRR}_{d,p} = \Pr \left( r_{d,p,b} \neq r_{d,\text{minimal},b} \right)
        \]
      }
    \end{column}
    \begin{column}{0.4\textwidth}
      \onslide<4->{
      \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.9em,wd=\linewidth]{block body}
        {\large \textbf{Interpretation}}\\[0.5em]
        Rank is not treated as a fixed truth, but as a distribution over plausible evaluation choices.
      \end{beamercolorbox}
      }
    \end{column}
  \end{columns}

  \onslide<5->{
    \vspace{0.8em}
    \insightbox{High RRR means a single leaderboard is a fragile snapshot rather than a stable conclusion.}
  }
\end{frame}

\begin{frame}{Experimental Setup}
  \begin{columns}[T]
    \begin{column}{0.49\textwidth}
      \onslide<1->{
      \textbf{Benchmarks}
      \begin{itemize}
        \item MMLU: 300 questions
        \item ARC-Challenge: 300 questions
        \item HellaSwag: 300 questions
      \end{itemize}

      \textbf{Prompt templates}
      \begin{itemize}
        \item \texttt{minimal\_instruction}
        \item \texttt{benchmark\_style}
        \item \texttt{natural\_style}
        \item \texttt{order\_phrasing\_variation}
      \end{itemize}
      }
    \end{column}
    \begin{column}{0.47\textwidth}
      \onslide<2->{
      \textbf{Models}
      \begin{itemize}
        \item GPT-4o-mini
        \item Gemini-2.0-flash
        \item Qwen-2.5-7B
        \item Llama-3.1-8B
      \end{itemize}

      \textbf{Inference / analysis}
      \begin{itemize}
        \item temperature = 0
        \item max\_tokens = 150
        \item 2000 bootstrap resamples per dataset
      \end{itemize}
      }
    \end{column}
  \end{columns}

  \onslide<3->{
    \vspace{0.8em}
    \begin{columns}[T]
      \begin{column}{0.24\textwidth}
        \metriccard{900}{questions}
      \end{column}
      \begin{column}{0.24\textwidth}
        \metriccard{3600}{prompt requests}
      \end{column}
      \begin{column}{0.24\textwidth}
        \metriccard{14400}{responses}
      \end{column}
      \begin{column}{0.24\textwidth}
        \metriccard{3}{parse failures}
      \end{column}
    \end{columns}
  }
\end{frame}

\begin{frame}{Pipeline}
  \centering
  \begin{tikzpicture}[
      node distance=0.55cm,
      box/.style={draw=Ink, fill=Soft, rounded corners, minimum height=1.2cm, minimum width=2.5cm, align=center},
      arr/.style={-{Latex[length=3mm]}, very thick, draw=Accent}
    ]
    \node[box] (q) {Sample\\questions};
    \node[box, right=of q] (p) {Generate 4\\prompt variants};
    \node[box, right=of p] (m) {Query 4\\models};
    \node[box, right=of m] (s) {Parse and\\score};
    \node[box, right=of s] (b) {Bootstrap\\rankings};

    \draw[arr] (q) -- (p);
    \draw[arr] (p) -- (m);
    \draw[arr] (m) -- (s);
    \draw[arr] (s) -- (b);
  \end{tikzpicture}

  \onslide<2->{
    \vspace{1em}
    \begin{itemize}[<+->]
      \item This design isolates two realistic sources of instability: \textbf{prompt choice} and \textbf{subset choice}.
      \item It also keeps the task fixed, so reversals cannot be dismissed as semantic task drift.
    \end{itemize}
  }
\end{frame}

\sectionframe{Results}{Three benchmarks, three very different stability stories}

\begin{frame}{Main Result: Ranking Stability Is Benchmark-dependent}
  \begin{columns}[T]
    \begin{column}{0.62\textwidth}
      \includegraphics[width=\linewidth]{figures/ranking_reversal_rate.png}
    \end{column}
    \begin{column}{0.34\textwidth}
      \onslide<2->{
        \textbf{RRR relative to the minimal prompt}
        \vspace{0.5em}
        \begin{itemize}[<+->]
          \item ARC: 4.70\%, 1.65\%, 1.05\%
          \item MMLU: 19.60\%, 8.05\%, 13.50\%
          \item HellaSwag: 62.55\%, 41.35\%, 38.50\%
        \end{itemize}
      }

      \onslide<5->{
        \vspace{0.5em}
        \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
          Same method, same models, same prompt philosophy; radically different stability.
        \end{beamercolorbox}
      }
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Reading the Accuracy Landscape}
  \includegraphics[width=\linewidth]{figures/accuracy_boxplots.png}

  \onslide<2->{
    \vspace{0.5em}
    \begin{itemize}[<+->]
      \item \textbf{ARC}: tight boxes, clear separation, wide performance margins.
      \item \textbf{HellaSwag}: larger prompt sensitivity, especially around the top two models.
      \item \textbf{MMLU}: clear winner, but less separation in the middle ranks.
    \end{itemize}
  }
\end{frame}

\begin{frame}{Case Study 1: HellaSwag and the Fragile Winner}
  \begin{columns}[T]
    \begin{column}{0.32\textwidth}
      \onslide<1->{
      \metriccard{62.55\%}{highest RRR under benchmark-style prompting}
      }
    \end{column}
    \begin{column}{0.32\textwidth}
      \onslide<2->{
      \metriccard{1.92\%}{GPT lead over Gemini in prompt-averaged accuracy}
      }
    \end{column}
    \begin{column}{0.32\textwidth}
      \onslide<3->{
      \metriccard{$p = 0.169$}{the GPT--Gemini gap is not significant}
      }
    \end{column}
  \end{columns}

  \onslide<4->{
    \vspace{0.9em}
    \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.9em,wd=\linewidth]{block body}
      \centering
      {\large \textbf{Interpretation}}\\[0.4em]
      HellaSwag provides the clearest counterexample to the static leaderboard narrative: the apparent winner exists at the point-estimate level, but not as a robust rank claim.
    \end{beamercolorbox}
  }
\end{frame}

\begin{frame}{Case Study 2: ARC-Challenge and a Robust Leaderboard}
  \begin{columns}[T]
    \begin{column}{0.54\textwidth}
      \begin{itemize}[<+->]
        \item Gemini is first under all prompt variants.
        \item All pairwise comparisons are significant.
        \item RRR remains close to zero across alternative prompts.
      \end{itemize}

      \onslide<4->{
        \vspace{0.8em}
        \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
          ARC behaves like the idealized leaderboard story: prompt and subset variation are too weak to overturn the ordering.
        \end{beamercolorbox}
      }
    \end{column}
    \begin{column}{0.42\textwidth}
      \onslide<2->{
      \metriccard{94.00\%}{Gemini prompt-averaged accuracy}
      }
      \onslide<3->{
        \vspace{0.4em}
      \metriccard{99\%+}{Gemini top-1 probability across prompts}
      }
      \onslide<4->{
        \vspace{0.4em}
      \metriccard{1--5\%}{RRR range only}
      }
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Case Study 3: MMLU and an Unstable Middle}
  \begin{columns}[T]
    \begin{column}{0.54\textwidth}
      \begin{itemize}[<+->]
        \item Gemini is clearly first and remains first under all prompt variants.
        \item But GPT vs Qwen is not statistically significant.
        \item So MMLU is not fully unstable; it is \textbf{hierarchically unstable}:
        the winner is settled, the middle ranks are not.
      \end{itemize}

      \onslide<4->{
        \vspace{0.8em}
        \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
          The right lesson is not ``MMLU is noisy,'' but ``different parts of the ranking have different certainty levels.''
        \end{beamercolorbox}
      }
    \end{column}
    \begin{column}{0.42\textwidth}
      \onslide<2->{
      \metriccard{11.67\%}{Gemini lead over GPT}
      }
      \onslide<3->{
        \vspace{0.4em}
      \metriccard{3.08\%}{GPT lead over Qwen}
      }
      \onslide<4->{
        \vspace{0.4em}
      \metriccard{$p = 0.184$}{GPT--Qwen gap is not significant}
      }
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{One More Layer: MMLU Is Also Subject-dependent}
  \begin{columns}[T]
    \begin{column}{0.62\textwidth}
      \includegraphics[width=\linewidth]{figures/mmlu_subject_rrr.png}
    \end{column}
    \begin{column}{0.34\textwidth}
      \begin{itemize}[<+->]
        \item Full MMLU preserves the same overall winner.
        \item But subject-level stability varies sharply.
        \item Mean non-minimal RRR ranges from about 14.5\% to 67.9\%.
      \end{itemize}

      \onslide<4->{
        \vspace{0.7em}
        \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
          Ranking robustness is benchmark-dependent \textbf{and} subdomain-dependent.
        \end{beamercolorbox}
      }
    \end{column}
  \end{columns}
\end{frame}

\sectionframe{Takeaways}{What this project says about how LLM evaluation should be reported}

\begin{frame}{What We Learned}
  \begin{enumerate}[<+->]
    \item Prompt perturbations and subset variation do not affect all benchmarks equally.
    \item Ranking reversals occur most often when performance gaps are narrow.
    \item A single leaderboard may be robust, partially stable, or fragile depending on the task.
    \item Average accuracy alone is not enough; rank uncertainty deserves explicit reporting.
  \end{enumerate}

  \onslide<5->{
    \vspace{0.9em}
    \insightbox{A better benchmark report should state not only who is best on average, but how robust that ordering is.}
  }
\end{frame}

\begin{frame}{Implications and Limitations}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \onslide<1->{
      \textbf{Implications}
      \begin{itemize}
        \item report confidence intervals and pairwise significance
        \item report ranking stability metrics such as RRR
        \item avoid overinterpreting narrow score gaps as stable superiority
      \end{itemize}
      }
    \end{column}
    \begin{column}{0.48\textwidth}
      \onslide<2->{
      \textbf{Limitations}
      \begin{itemize}
        \item only 3 multiple-choice benchmarks
        \item only 4 models
        \item prompt perturbations are controlled and shallow, not exhaustive
      \end{itemize}
      }
    \end{column}
  \end{columns}

  \onslide<3->{
    \vspace{0.9em}
    \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
      This is not a claim that all leaderboards are unreliable. It is a claim that reliability must be demonstrated, not assumed.
    \end{beamercolorbox}
  }
\end{frame}

\begin{frame}{Conclusion}
  \begin{columns}[T]
    \begin{column}{0.32\textwidth}
      \onslide<1->{
      \metriccard{ARC}{robust leaderboard}
      }
    \end{column}
    \begin{column}{0.32\textwidth}
      \onslide<2->{
      \metriccard{MMLU}{stable winner, unstable middle}
      }
    \end{column}
    \begin{column}{0.32\textwidth}
      \onslide<3->{
      \metriccard{HellaSwag}{fragile top rank}
      }
    \end{column}
  \end{columns}

  \onslide<4->{
    \vspace{1em}
    \insightbox{The central question is not only ``Who is best?'' but ``Does that ordering survive reasonable evaluation variation?''}
  }
\end{frame}

\sectionframe{Experiment Design and Result}{Experimental workflow, metric definitions, and reversal cases we found}

\begin{frame}{Pipeline}
	\centering
	\begin{tikzpicture}[
		node distance=0.55cm,
		box/.style={draw=Ink, fill=Soft, rounded corners, minimum height=1.2cm, minimum width=2.5cm, align=center},
		arr/.style={-{Latex[length=3mm]}, very thick, draw=Accent}
		]
		\node[box] (q) {Sample\\questions};
		\node[box, right=of q] (p) {Generate 4\\prompt variants};
		\node[box, right=of p] (m) {Query 4\\models};
		\node[box, right=of m] (s) {Parse and\\score};
		\node[box, right=of s] (b) {Analyze by\\bootstraping};
		
		\draw[arr] (q) -- (p);
		\draw[arr] (p) -- (m);
		\draw[arr] (m) -- (s);
		\draw[arr] (s) -- (b);
	\end{tikzpicture}
	
	\begin{columns}[T]
		\begin{column}{0.95\textwidth}
			\begin{itemize}[<+->]
				\item Phase 1: samples \textbf{300 items} for each dataset and expands each item into \textbf{4 prompt templates}. For MMLU, we selected \textbf{6 distinct subjects} and sampled \textbf{50 items} from each subject.
				\item Phase 2: queries 4 models on all prompt requests, parses each of the 14,400 responses into A/B/C/D, and turn it into a binary score.
				\item Phase 3: By bootstraping, compute model accuracy and ranking reversal rates (see next slide).
			\end{itemize}
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}{Definition of Core Statistics: Accuracy and RRR's}
	
  \begin{itemize}[<+->]
    \item \textbf{Accuracy}: for any item set $A$ and template $T$, a model's accuracy on $A$ under $T$ is the mean of its fixed binary scores when asked problems from $A$ with template $T$.
    \item \textbf{Ranking}: for any item set $A$ and template $T$, the 4 models can be sorted by their accuracy on $A$ under $T$, inducing a ranking, denoted $R_{A,T}$.
    \item \textbf{Prompt-wise Ranking Reversal Rate (PRRR)}: For an item set $P$, two templates $T_1$ and $T_2$, draw $N$ subsets $P_i \subset P$; the PRRR on $P$ between $T_1$ and $T_2$ is the frequency of different rankings occuring under two templates among the $N$ subsets, denote PRRR($P, T_1, T_2$). i.e.,
    $$ \text{PRRR}(P, T_1, T_2):= \# \{i \in [1:N] : R_{P_i, T_1} \neq R_{P_i, T_2}\} / N.$$
    \item \textbf{Subset Ranking Reversal Rate (SRRR)}: For an item set $P$ and template $T$, draw $N$ subsets $P_i \subset P$; the SRRR on $P$ under $T$ is the frequency of different rankings occuring under $T$ when asked subsets and the full set, denote SRRR($P, T$). i.e.,
    $$\text{SRRR}(P, T) := \# \{i \in [1:N] : R_{P,T} \neq R_{P_i,T}\} / N.$$
  \end{itemize}
\end{frame}

\begin{frame}{Definition of Core Statistics: Accuracy and RRR's}
	\insightbox{We isolate two sources of instability: \textbf{prompt choice} and \textbf{incomplete test set}. \\
	PRRR measures prompt-to-prompt rank disagreement on the same task set; SRRR measures full-set vs subset rank instability under the same prompt.}
	\begin{columns}[T]
		\onslide<2->{
		\begin{column}{0.9\textwidth}
			Experiment settings:
			\begin{itemize}
				\item We applied bootstrap number $N = 1000$, and drew half-sized subsets: $\# P_i = \# P /2$.
				\item For 3 datasets (MMLU, ARC-Challenge, and HellaSwag), and 6 subjects of question sets from MMLU, we computed:
				\begin{itemize}
					\item PRRR between minimal instruction and the other 3 templates;
					\item SRRR under all 4 templates.
				\end{itemize}
			\end{itemize}
		\end{column}
		}
	\end{columns}
	
\end{frame}

\begin{frame}{Experiment Results: Dataset-Level Ranking Instability}
	\begin{columns}[T]
		\begin{column}{\textwidth}
			\includegraphics[width=\linewidth]{figures/accuracy_boxplots.png}
		\end{column}
		%\begin{column}{0.48\textwidth}
		%  \includegraphics[width=\linewidth]{figures/hellaswag_rank_probability_heatmaps.png}
		%\end{column}
	\end{columns}
	\vspace{0.6em}
	\begin{columns}[T]
		\begin{column}{0.95\textwidth}
			The accuracy boxplots show that Gemini leads on ARC-Challenge and MMLU, while GPT-4o-mini leads on HellaSwag.
			%\item The HellaSwag heatmap shows why this benchmark is fragile: GPT is usually rank 1, but the rest of the ordering remains prompt-sensitive, which is consistent with the high PRRR and SRRR values.
			%\item Observation: even when the top model often stays ahead, .
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}{Experiment Results: Dataset-Level Ranking Instability}
	\begin{columns}[T]
		\begin{column}{0.33\textwidth}
			\centering
			\includegraphics[width=\linewidth]{figures/arc_challenge_rank_probability_heatmaps.png}
			\vspace{0.3em}
			{\scriptsize Ranking heatmap (ARC)}
		\end{column}
		
		\begin{column}{0.33\textwidth}
			\centering
			\includegraphics[width=\linewidth]{figures/hellaswag_rank_probability_heatmaps.png}
			\vspace{0.3em}
			{\scriptsize Ranking heatmap (HellaSwag)}
		\end{column}
		
		\begin{column}{0.33\textwidth}
			\centering
			\includegraphics[width=\linewidth]{figures/mmlu_rank_probability_heatmaps.png}
			\vspace{0.3em}
			{\scriptsize Ranking heatmap (MMLU)}
		\end{column}
	\end{columns}
	\vspace{1.5em}
	\begin{columns}[T]
		\begin{column}{0.9\textwidth}
			\begin{itemize}[<+->]
				\item The darker a tile, the more likely a model falls into that place in a ranking.
				\item Distribution of model rankings differ among datasets and prompt styles.
			\end{itemize}
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}{Experiment Results: Dataset-Level RRR's}
	\begin{columns}[T]
  	
	  	\begin{column}{0.6\textwidth}
	  		\includegraphics[width=\linewidth]{figures/prompt_ranking_reversal_rate.png}
	  	\end{column}
    
		\begin{column}{0.38\textwidth}
    		PRRR Results:
    		\begin{itemize}
	        	\item ARC-Challenge:	\\
	        	 max PRRR = 10.6\%;
	        	\item HellaSwag:		\\
	        	 max PRRR = 56.0\%;
	        	\item MMLU:				\\
	        	 max PRRR = 15.6\%.
			\end{itemize}
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}{Experiment Results: Dataset-Level RRR's}
	\begin{columns}[T]
		
		\begin{column}{0.6\textwidth}
			\includegraphics[width=\linewidth]{figures/subset_ranking_reversal_rate.png}
		\end{column}
		
		\begin{column}{0.38\textwidth}
			SRRR Results:
			\begin{itemize}
				\item ARC-Challenge:	\\
				max SRRR = 11.2\%
				\item HellaSwag:		\\
				max SRRR = 42.8\%
				\item MMLU:				\\
				max SRRR = 17.5\%
			\end{itemize}
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}{Experiment Results: Dataset-Level RRR's}
  \begin{columns}[T]
    
    \begin{column}{0.9\textwidth}
      \begin{itemize}
        \item \textbf{HellaSwag}: strongest dataset-level reversal case, especially under T2 Benchmark.
        \item \textbf{MMLU}: overall winner is stable, but middle ranks are more sensitive to subset changes.
        \item \textbf{ARC}: rank order survives both prompt and subset variation in most resamples.
      \end{itemize}
      \onslide<2->{
      	\vspace{1.5em}
      	\begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
      		HellaSwag is the clearest fragile-leaderboard case. ARC-Challenge remains the most stable benchmark.
      	\end{beamercolorbox}
      }
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Experiment Results: MMLU Subject-Level Ranking Instability}
	\begin{columns}[T]
		\begin{column}{\textwidth}
			\centering
			\includegraphics[width=0.85\linewidth]{figures/mmlu_subject_expanded_accuracy_boxplots.png}
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}{Experiment Results: MMLU Subject-Level Ranking Instability}
	\begin{columns}[T]
		\begin{column}{0.33\textwidth}
			\centering
			\includegraphics[width=\linewidth]{figures/mmlu_subject_expanded_MMLU_S1_rank_probability_heatmaps.png}
			{\scriptsize high\_school\_mathematics}
		\end{column}
		\begin{column}{0.33\textwidth}
			\centering
			\includegraphics[width=\linewidth]{figures/mmlu_subject_expanded_MMLU_S3_rank_probability_heatmaps.png}
			{\scriptsize high\_school\_world\_history}
		\end{column}
		\begin{column}{0.33\textwidth}
			\centering
			\includegraphics[width=\linewidth]{figures/mmlu_subject_expanded_MMLU_S5_rank_probability_heatmaps.png}
			{\scriptsize sociology}
		\end{column}
	\end{columns}
	\vspace{1.75em}
	\begin{columns}[T]
		\begin{column}{0.9\textwidth}
			Heatmaps show the rankings vary a lot among subjects and prompt styles.
			\begin{itemize}[<+->]
				\item Model ranking distributions are very different among all 3 example subjects.
				\item Different prompts can break a 50-50 tie between two models.
			\end{itemize}
		\end{column}
	\end{columns}
\end{frame}

\begin{frame}{Experiment Results: MMLU Subject-Level RRR's}
  \begin{columns}[T]
    \begin{column}{0.63\textwidth}
      \includegraphics[width=\linewidth]{figures/mmlu_subject_expanded_prrr.png}
      \includegraphics[width=\linewidth]{figures/mmlu_subject_expanded_srrr.png}
    \end{column}
    \begin{column}{0.35\textwidth}
      \begin{itemize}[<+->]
        \item Largest PRRR: \texttt{high\_school\_world\_history}, T1 vs T3 = 74.30\%
        \item Largest SRRR: \texttt{sociology}, T1 = 67.90\%
      \end{itemize}

      \onslide<3->{
      \begin{beamercolorbox}[rounded=true,shadow=true,sep=0.8em,wd=\linewidth]{block body}
        The RRR's show that subject-level ranking instability is even higher than dataset-Level instability.
      \end{beamercolorbox}
      }
    \end{column}
  \end{columns}
\end{frame}

\end{document}
